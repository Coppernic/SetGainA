<?xml version="1.0" encoding="utf-8"?>
<manifest package="fr.coppernic.fixgaina"
          xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@drawable/rfid_blue"
        android:label="@string/app_name"
        android:roundIcon="@drawable/rfid_blue"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".home.HomeActivity">
        </activity>
        <activity android:name="fr.coppernic.lib.splash.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <meta-data
                android:name="activity"
                android:value="fr.coppernic.fixgaina.home.HomeActivity" />
        </activity>
    </application>

</manifest>